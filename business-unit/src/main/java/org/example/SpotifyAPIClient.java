package org.example;

import org.example.model.Artist;

public class SpotifyAPIClient {

    public void createPlaylistForArtist(Artist artist) {
        // Aquí iría la lógica para crear una playlist en Spotify para el artista
        // Usando la API de Spotify (usualmente a través de OAuth2)
        System.out.println("Creando playlist para el artista: " + artist.getName());
    }
}

